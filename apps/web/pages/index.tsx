import xwind from 'xwind';

/**
 * Index page.
 * @return {JSX.Element}
 */
export default function Index() {
    return (
        <div css={xwind`text-center`}>
            <p css={xwind`text-4xl`}>Hello World</p>
        </div>
    );
}
